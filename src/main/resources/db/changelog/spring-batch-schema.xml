<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!-- Spring Batch 5 PostgreSQL Schema -->

    <!-- BATCH_JOB_INSTANCE 表 -->
    <changeSet id="20260131-create-batch-job-instance" author="libre">
        <preConditions onFail="MARK_RAN">
            <not>
                <tableExists tableName="batch_job_instance"/>
            </not>
        </preConditions>
        <sql>
            CREATE TABLE BATCH_JOB_INSTANCE (
                JOB_INSTANCE_ID BIGINT NOT NULL PRIMARY KEY,
                VERSION BIGINT,
                JOB_NAME VARCHAR(100) NOT NULL,
                JOB_KEY VARCHAR(32) NOT NULL,
                CONSTRAINT JOB_INST_UN UNIQUE (JOB_NAME, JOB_KEY)
            );
        </sql>
    </changeSet>

    <!-- BATCH_JOB_EXECUTION 表 -->
    <changeSet id="20260131-create-batch-job-execution" author="libre">
        <preConditions onFail="MARK_RAN">
            <not>
                <tableExists tableName="batch_job_execution"/>
            </not>
        </preConditions>
        <sql>
            CREATE TABLE BATCH_JOB_EXECUTION (
                JOB_EXECUTION_ID BIGINT NOT NULL PRIMARY KEY,
                VERSION BIGINT,
                JOB_INSTANCE_ID BIGINT NOT NULL,
                CREATE_TIME TIMESTAMP NOT NULL,
                START_TIME TIMESTAMP DEFAULT NULL,
                END_TIME TIMESTAMP DEFAULT NULL,
                STATUS VARCHAR(10),
                EXIT_CODE VARCHAR(2500),
                EXIT_MESSAGE VARCHAR(2500),
                LAST_UPDATED TIMESTAMP,
                CONSTRAINT JOB_INST_EXEC_FK FOREIGN KEY (JOB_INSTANCE_ID)
                    REFERENCES BATCH_JOB_INSTANCE(JOB_INSTANCE_ID)
            );
        </sql>
    </changeSet>

    <!-- BATCH_JOB_EXECUTION_PARAMS 表 -->
    <changeSet id="20260131-create-batch-job-execution-params" author="libre">
        <preConditions onFail="MARK_RAN">
            <not>
                <tableExists tableName="batch_job_execution_params"/>
            </not>
        </preConditions>
        <sql>
            CREATE TABLE BATCH_JOB_EXECUTION_PARAMS (
                JOB_EXECUTION_ID BIGINT NOT NULL,
                PARAMETER_NAME VARCHAR(100) NOT NULL,
                PARAMETER_TYPE VARCHAR(100) NOT NULL,
                PARAMETER_VALUE VARCHAR(2500),
                IDENTIFYING CHAR(1) NOT NULL,
                CONSTRAINT JOB_EXEC_PARAMS_FK FOREIGN KEY (JOB_EXECUTION_ID)
                    REFERENCES BATCH_JOB_EXECUTION(JOB_EXECUTION_ID)
            );
        </sql>
    </changeSet>

    <!-- BATCH_STEP_EXECUTION 表 -->
    <changeSet id="20260131-create-batch-step-execution" author="libre">
        <preConditions onFail="MARK_RAN">
            <not>
                <tableExists tableName="batch_step_execution"/>
            </not>
        </preConditions>
        <sql>
            CREATE TABLE BATCH_STEP_EXECUTION (
                STEP_EXECUTION_ID BIGINT NOT NULL PRIMARY KEY,
                VERSION BIGINT NOT NULL,
                STEP_NAME VARCHAR(100) NOT NULL,
                JOB_EXECUTION_ID BIGINT NOT NULL,
                CREATE_TIME TIMESTAMP NOT NULL,
                START_TIME TIMESTAMP DEFAULT NULL,
                END_TIME TIMESTAMP DEFAULT NULL,
                STATUS VARCHAR(10),
                COMMIT_COUNT BIGINT,
                READ_COUNT BIGINT,
                FILTER_COUNT BIGINT,
                WRITE_COUNT BIGINT,
                READ_SKIP_COUNT BIGINT,
                WRITE_SKIP_COUNT BIGINT,
                PROCESS_SKIP_COUNT BIGINT,
                ROLLBACK_COUNT BIGINT,
                EXIT_CODE VARCHAR(2500),
                EXIT_MESSAGE VARCHAR(2500),
                LAST_UPDATED TIMESTAMP,
                CONSTRAINT JOB_EXEC_STEP_FK FOREIGN KEY (JOB_EXECUTION_ID)
                    REFERENCES BATCH_JOB_EXECUTION(JOB_EXECUTION_ID)
            );
        </sql>
    </changeSet>

    <!-- BATCH_STEP_EXECUTION_CONTEXT 表 -->
    <changeSet id="20260131-create-batch-step-execution-context" author="libre">
        <preConditions onFail="MARK_RAN">
            <not>
                <tableExists tableName="batch_step_execution_context"/>
            </not>
        </preConditions>
        <sql>
            CREATE TABLE BATCH_STEP_EXECUTION_CONTEXT (
                STEP_EXECUTION_ID BIGINT NOT NULL PRIMARY KEY,
                SHORT_CONTEXT VARCHAR(2500) NOT NULL,
                SERIALIZED_CONTEXT TEXT,
                CONSTRAINT STEP_EXEC_CTX_FK FOREIGN KEY (STEP_EXECUTION_ID)
                    REFERENCES BATCH_STEP_EXECUTION(STEP_EXECUTION_ID)
            );
        </sql>
    </changeSet>

    <!-- BATCH_JOB_EXECUTION_CONTEXT 表 -->
    <changeSet id="20260131-create-batch-job-execution-context" author="libre">
        <preConditions onFail="MARK_RAN">
            <not>
                <tableExists tableName="batch_job_execution_context"/>
            </not>
        </preConditions>
        <sql>
            CREATE TABLE BATCH_JOB_EXECUTION_CONTEXT (
                JOB_EXECUTION_ID BIGINT NOT NULL PRIMARY KEY,
                SHORT_CONTEXT VARCHAR(2500) NOT NULL,
                SERIALIZED_CONTEXT TEXT,
                CONSTRAINT JOB_EXEC_CTX_FK FOREIGN KEY (JOB_EXECUTION_ID)
                    REFERENCES BATCH_JOB_EXECUTION(JOB_EXECUTION_ID)
            );
        </sql>
    </changeSet>

    <!-- BATCH_STEP_EXECUTION_SEQ 序列 -->
    <changeSet id="20260131-create-batch-step-execution-seq" author="libre">
        <preConditions onFail="MARK_RAN">
            <not>
                <sequenceExists sequenceName="batch_step_execution_seq"/>
            </not>
        </preConditions>
        <sql>
            CREATE SEQUENCE BATCH_STEP_EXECUTION_SEQ MAXVALUE 9223372036854775807 NO CYCLE;
        </sql>
    </changeSet>

    <!-- BATCH_JOB_EXECUTION_SEQ 序列 -->
    <changeSet id="20260131-create-batch-job-execution-seq" author="libre">
        <preConditions onFail="MARK_RAN">
            <not>
                <sequenceExists sequenceName="batch_job_execution_seq"/>
            </not>
        </preConditions>
        <sql>
            CREATE SEQUENCE BATCH_JOB_EXECUTION_SEQ MAXVALUE 9223372036854775807 NO CYCLE;
        </sql>
    </changeSet>

    <!-- BATCH_JOB_SEQ 序列 -->
    <changeSet id="20260131-create-batch-job-seq" author="libre">
        <preConditions onFail="MARK_RAN">
            <not>
                <sequenceExists sequenceName="batch_job_seq"/>
            </not>
        </preConditions>
        <sql>
            CREATE SEQUENCE BATCH_JOB_SEQ MAXVALUE 9223372036854775807 NO CYCLE;
        </sql>
    </changeSet>

</databaseChangeLog>
